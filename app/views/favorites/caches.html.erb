<div class="container">
  <% @caches.each do |cache| %>
  <div class="container bg-white p-2 m-5 d-flex">
    <%= cache.seed_photo == nil ? "" : image_tag("#{cache.seed_photo}", size: "60x60") %>
    <div class="container">
      <%= link_to cache.title, cache_path(cache) %>
      <p>@<%= cache.user.username %></p>
    </div>
  </div>
  <% end %>
</div>
